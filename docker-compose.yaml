services:
  marva-quartz:
    build:
      context: .
      args:
        VITE_KEYCLOAK_AUTH_PATH: "http://localhost:8081/keycloak/"
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
